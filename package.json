{
  "name": "agent-company-wuwa",
  "version": "2.0.0",
  "description": "鸣潮世界观的 Agent 元宇宙 - 基于 Miniverse 架构",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "server": "node --experimental-strip-types server/index.ts",
    "server:dev": "node --watch --experimental-strip-types server/index.ts",
    "preview": "vite preview"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/ws": "^8.5.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
