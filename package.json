{
  "name": "agent-company-wuwa",
  "version": "3.0.0",
  "description": "鸣潮主题动态像素世界 Agent-Company v3 - 基于 Miniverse 架构",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "server": "node --experimental-strip-types server/index.ts",
    "server:dev": "node --watch --experimental-strip-types server/index.ts",
    "preview": "vite preview"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/ws": "^8.5.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
